## Files To Read

- Do not read `{baseDir}/config/agents-workspace-api-access.config.json` directly. The proxy tool script reads it automatically; never print or expose its contents.
- In paths below, replace `<workspace-email>` with the Workspace account email address exactly as listed in the `Workspace Selection` section of this `SKILL.md`.
- Read `{baseDir}/skills/<workspace-email>/GMAIL.md` first for Gmail tasks.
- Read `{baseDir}/skills/<workspace-email>/CONTACTS.md` first when the user gives a partial person name, a recipient hint, or any request that depends on identifying the correct contact before proceeding.
- Read `{baseDir}/skills/<workspace-email>/DRIVE.md` first for Drive, Docs, Sheets, or Slides tasks.
- Read `{baseDir}/skills/<workspace-email>/CALENDAR.md` for Calendar tasks.
