Only the capability sections below are available for this workspace because they match the applied proxy policy.

Commands shown here are helper examples. If a requested Google API operation fits an available capability but no helper example is shown, use full passthrough mode with the official Google Workspace API documentation.
